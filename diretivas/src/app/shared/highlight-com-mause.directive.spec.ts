import { HighlightComMauseDirective } from './highlight-com-mause.directive';

describe('HighlightComMauseDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightComMauseDirective();
    expect(directive).toBeTruthy();
  });
});
